export interface CharacterProps {
   data: Array<{
      name:string
      age:string
      gender:string
      race:string
   }>
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
function init_char(p:CharacterProps) {}